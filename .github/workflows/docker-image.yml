name: Docker Push

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: mandarin3d/itsmejessicalee-docker:latest, mandarin3d/itsmejessicalee-docker:${{ github.sha }}

    - name: Send JSON to API endpoint
      run: |
        curl -X PUT "https://api.example.com/endpoint" \
        -H "Authorization: Bearer ${{ secrets.BEARER_TOKEN }}" \
        -H "Content-Type: application/json" \
        -d '{"dc_name": "itsmejessicalee", "dc_ip": "192.168.10.80", "dc_port": 5450, "dc_image": "mandarin3d/itsmejessicalee-docker"}'
